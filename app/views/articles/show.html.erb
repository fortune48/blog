<h2><%= @article.title %></h2>

<p><%= @article.body %></p>

<%= link_to "Edit Article", edit_article_path(@article) %> | <%= link_to "Delete Article", article_path(@article), data: {
                                    turbo_method: :delete, turbo_confirm: "You sure?" }%> | <%= link_to "Back", root_path %>